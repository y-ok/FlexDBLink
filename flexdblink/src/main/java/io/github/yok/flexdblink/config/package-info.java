/**
 * Configuration model package for FlexDBLink.
 *
 * <p>
 * Defines classes that represent values loaded from {@code application.yml} (or equivalent
 * sources), such as connection settings, path settings, load/dump settings, and file naming rules.
 * </p>
 *
 * <p>
 * This package primarily holds configuration data; execution logic is implemented in {@code core}
 * and {@code db}.
 * </p>
 */
package io.github.yok.flexdblink.config;
